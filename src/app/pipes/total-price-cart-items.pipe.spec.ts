import {TotalPriceCartItemsPipe} from './total-price-cart-items.pipe';

describe('TotalPriceCartItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalPriceCartItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
